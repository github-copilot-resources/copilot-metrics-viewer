name: Build and push Docker image with release tag

on:
  
  release:
    types: 
      - published

  # Allows you to run this workflow manually from the Actions tab
  # Provide the tag name (e.g. v3.6.2) to re-run a release without deleting it.
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to build (e.g. v3.6.2)'
        required: true
        type: string

permissions:
  packages: write
  contents: read
  pull-requests: read

jobs:
  # version_update:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout GitHub Action
  #       uses: actions/checkout@v4
  #       with:
  #         fetch-depth: 0

  #     - uses: actions4git/setup-git@v1
      
  #     - name: Update version in package.json
  #       id: version
  #       run: |
  #         git checkout main
  #         version=$(npm version from-git --no-git-tag-version)
  #         echo "version=$version" >> $GITHUB_OUTPUT
  #         echo "NPM version is $version"

  #         if [ "${{ github.event.release.tag_name || inputs.tag }}" != "$version" ]; then
  #           echo "Release tag does not match the expected version."
  #           exit 1
  #         fi

  #         git add .
  #         git commit --signoff --message $version

  #     - name: Create Pull Request
  #       uses: peter-evans/create-pull-request@v7
  #       continue-on-error: true
  #       with:
  #         title: '🚀Update version to ${{ steps.version.outputs.version }}'
  #         body: |
  #           This PR updates the version to ${{ steps.version.outputs.version }}. 
  #           Use **merge commit** strategy for this PR!
  #         branch: 'version-update-${{ steps.version.outputs.version }}'
  #         delete-branch: true
  #         labels: 'version-update'
  #         base: 'main'
      
  #     - name: Move tag ${{ steps.version.outputs.version }} to PR
  #       run: |
  #         echo "moving tag ${{ steps.version.outputs.version }}"
  #         git tag --force ${{ steps.version.outputs.version }}
  #         git push origin ${{ steps.version.outputs.version }} --force

  push_to_ghcr:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    # needs: version_update
    steps:
      - name: Checkout GitHub Action
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name || inputs.tag }}

      # Fail fast if the release tag doesn't match package.json version.
      # Fix: bump the version in a commit before pushing the release tag.
      - name: Verify npm version matches release tag
        run: |
          TAG="${{ github.event.release.tag_name || inputs.tag }}"
          TAG="${TAG#v}"
          PKG=$(node -p "require('./package.json').version")
          if [ "$TAG" != "$PKG" ]; then
            echo "::error::Tag ${{ github.event.release.tag_name || inputs.tag }} requires package.json version $TAG but found $PKG. Bump the version before releasing."
            exit 1
          fi
          echo "✓ package.json version $PKG matches tag ${{ github.event.release.tag_name || inputs.tag }}"

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/${{ github.repository }}
            ghcr.io/${{ github.repository }}-with-proxy
          labels: |
            org.opencontainers.image.title=copilot-metrics-viewer
            org.opencontainers.image.description=Metrics viewer for GitHub Copilot usage
            org.opencontainers.image.source=${{ github.repository }}
          tags: |
            type=raw,value=latest,enable=true
            type=sha
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          push: true
          provenance: false

  push_sync_to_ghcr:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: Checkout GitHub Action
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.release.tag_name || inputs.tag }}

      # Fail fast if the release tag doesn't match package.json version.
      # Fix: bump the version in a commit before pushing the release tag.
      - name: Verify npm version matches release tag
        run: |
          TAG="${{ github.event.release.tag_name || inputs.tag }}"
          TAG="${TAG#v}"
          PKG=$(node -p "require('./package.json').version")
          if [ "$TAG" != "$PKG" ]; then
            echo "::error::Tag ${{ github.event.release.tag_name || inputs.tag }} requires package.json version $TAG but found $PKG. Bump the version before releasing."
            exit 1
          fi
          echo "✓ package.json version $PKG matches tag ${{ github.event.release.tag_name || inputs.tag }}"

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ghcr.io/${{ github.repository }}-sync
          labels: |
            org.opencontainers.image.title=copilot-metrics-viewer-sync
            org.opencontainers.image.description=Sync service for GitHub Copilot metrics data
            org.opencontainers.image.source=${{ github.repository }}
          tags: |
            type=raw,value=latest,enable=true
            type=sha
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Build and push sync image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.sync
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          push: true
          provenance: false
